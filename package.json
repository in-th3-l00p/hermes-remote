{
  "name": "hermes-web",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "apps/examples/*",
    "integration"
  ],
  "scripts": {
    "test": "bun test packages",
    "test:integration": "cd integration && HERMES_INTEGRATION=1 bun test",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  }
}
